name: build and test 
on:
  workflow_dispatch:  
jobs:
  We-are-Going-to-Perform-CI:
    runs-on: ubuntu-latest
    steps:
      - name: installing npm dependencies
        run: npm install
        
    
